apiVersion: v1
kind: Service
metadata:
  name: casino-bonos
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: casino-bonos
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP